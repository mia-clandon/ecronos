html
    head
        meta(name="viewport" content="width=device-width, initial-scale=1")
        title Ecronos
        link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css..")
        script(src="https://code.jquery.com/jquery-1.6.min.js" integrity="sha256-5Y2lizFMze76PEhltLiqMVPokNeQTgRINIHY//LCfqo=" crossorigin="anonymous")
        link(rel="stylesheet" href="/stylesheets/index.css")
        link(rel="stylesheet" href="/stylesheets/style.css")
        link(rel="stylesheet" href="/stylesheets/card_news.css")
        link(rel="stylesheet" href="/stylesheets/news.css")
        link(rel="stylesheet" href="/stylesheets/template.css")
        link(rel="stylesheet" href="/stylesheets/contacts.css")
        link(rel="stylesheet" href="/stylesheets/show-news.css")
        link(rel="shortcut icon" type="image/png" href="/images/index/favicon.png")
    include navbar.pug
    .nav_header
        a(href="/") Main page/
        a(href="/en/services") Services/
        a#header(href="/en/services/read-services/"+ id) #{en_heading}
        script.
            $('#header').each(function () {
                var $this = $(this);
                var t = $this.text();
                $this.html(t.replace('&lt', '<').replace('&gt', '>'));
            });
        h1#header_ser.header_ser #{en_heading}
        script.
            $('#header_ser').each(function () {
                var $this = $(this);
                var t = $this.text();
                $this.html(t.replace('&lt', '<').replace('&gt', '>'));
            });
        .service_content
            #service_content_text.service_content_text  #{en_description}
        script.
            $('#service_content_text').each(function(){
                var $this = $(this);
                var t = $this.text();
            $this.html(t.replace('&lt','<').replace('&gt', '>'));
            });
